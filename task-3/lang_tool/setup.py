from setuptools import setup, find_packages

setup(
    name='lang_tool',
    version='1.0.0',
    author='sarvar',
    description='Lang tool is implementation of task 3 in computer-system-architecture (dynamic typing edition)',
    packages=find_packages()
)
